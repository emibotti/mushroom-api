<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
    <style>
      .mycelium-tile {
        border: 2px solid #000;
        padding: 10px;
        float: left;
        margin-right: 2%;
        margin-bottom: 20px;
      }
      .mycelium-image {
        width: 250px;
        height: 250px;
      }
      .mycelium-details {
        max-width: 250px;
        margin: 0 auto;
        text-align: center;
      }
      .clearfix::after {
        content: "";
        display: table;
        clear: both;
      }
    </style>
  </head>
  <body>
    <div class="clearfix">
      <% @mycelia.each_slice(3) do |mycelium_batch| %>
        <div class="clearfix">
          <% mycelium_batch.each do |mycelium| %>
            <div class="mycelium-tile">
              <img class="mycelium-image" src="<%= Rails.root.join('tmp', "#{mycelium.name}.svg") %>" alt="QR Code">

              <div class="mycelium-details">
                <p>
                  Mycelium Name: <%= mycelium.name %>
                </p>
                <p>
                  Species: <%= mycelium.species %>
                </p>
                <p>
                  Inoculation Date: <%= mycelium.inoculation_date %>
                </p>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </body>
</html>
